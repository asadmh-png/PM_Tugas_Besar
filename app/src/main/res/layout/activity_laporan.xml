<?xml version="1.0" encoding="utf-8"?>
<!-- Layout untuk Menampilkan Laporan Penjualan per Shift -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F0F2F5">

    <!-- Header dengan Toolbar untuk navigasi dan judul laporan -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbarLaporan"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/white"
        app:title="Laporan Penjualan (Shift)"
        app:navigationIcon="@android:drawable/ic_menu_revert"
        app:titleTextColor="#0D47A1" />

    <!-- Daftar laporan yang ditampilkan menggunakan RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvLaporanShift"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:clipToPadding="false" />

</LinearLayout>
